{
  "name": "diffhtml-prollyfill",
  "version": "0.0.0",
  "description": "Exposes an experimental browser API for diffHTML",
  "main": "index.js",
  "scripts": {
    "build": "browserify -g babelify -s diff index.js -o dist/diff-prollyfill.js",
    "watch": "watchify -g babelify -s diff index.js -o dist/diff-prollyfill.js -v"
  },
  "keywords": [],
  "author": "Tim Branyen (@tbranyen)",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1"
  },
  "peerDependencies": {
    "diffhtml": "*"
  }
}
